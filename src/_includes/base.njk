<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} - {% endif %}QUALITAT</title>
    <link href="/css/styles.css" rel="stylesheet">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Gantari&display=swap" rel="stylesheet">
<link rel="icon" href="/images/iso-icon.png" type="image/x-icon">
</head>
<body class="{{ class | default('') }}">
    {% include 'header.njk' %}
    <main id="content">
        {{ content | safe }}
    </main>
    {% include 'footer.njk' %}

    <script src="/js/main.js" defer></script>
</body>
</html>